# Changelog

本文档记录 Markdown Browser 的所有版本改动，按时间倒序排列。

## v2.3.0 — 图片预览修复 & 性能优化 (2026-07)

### 新增
- **本地图片预览** — markdown 中的相对路径图片（`images/foo.png`）现在可以从 md 文件所在目录通过文件夹句柄读取并渲染，不再破图标
- **openFile 失败提示** — 点击已删除/失效的文件时，状态栏提示"文件不存在"，不再静默无反应
- **表格列数对齐** — 某行多打了 `|` 导致列数比表头多时，多余内容自动合并到最后一格，不再静默丢失

### 修复
- **图片解析支持空格/中文路径** — `![截图 1](images/截图 1.png)` 之前显示源码，现在正常渲染（正则允许空格）
- **行内编辑退出弹窗** — 编辑已直接生效，退出时不再弹"放弃未保存内容"的误导确认
- **行内编辑入口检查时机** — 点击编辑按钮不再因无文件句柄弹提示，改为保存（⌘S）时才提示
- **表格最后一行泄漏** — 无尾随换行时最后一行数据掉出表格，解析器自动补 `\n`

### 性能
- **搜索索引分批构建** — 大文件夹不再阻塞 UI（每批 20 个文件 + 让出主线程）
- **减少内存占用** — 去掉 `rawContent` 字段（不再缓存全文原文），搜索片段改为按需读取
- **搜索防竞态** — 快速输入时旧结果不会覆盖新结果

---

## v2.2.0 — Mermaid 图表 & 多语言 (2026-07-09)

### 新增
- **Mermaid 流程图渲染** — ````mermaid` 代码块自动渲染为 SVG 图表
- **多语言支持 (i18n)** — 中英文实时切换，浏览器语言自动检测，设置面板切换，130+ 翻译键
- **顶部固定 + 滚动透明** — 面包屑/元信息栏 sticky 固定，滚动时透明度 40%
- **拖拽 .md 直接打开完整界面** — 拖文件进 Chrome 重定向到完整 SPA，所有功能可用
- **单文件模式** — 拖入文件即渲染，可点击"打开文件夹"挂载目录获取完整功能
- **最近记录直接打开** — FileSystemHandle 存 IndexedDB，点击直接恢复句柄打开，无需挂载文件夹
- **复制完整路径** — 从 file:// URL 提取完整磁盘路径，文件夹场景拼接文件夹名

### 修复
- **中文标题 TOC 无法定位** — slugify 支持中文 Unicode 范围
- **TOC 点击被 sticky header 遮挡** — scrollTo 加偏移量
- **scrollTop 重复声明崩溃** — 修复导致整个 app.js 无法加载的语法错误
- **拖入文件自动打开旧文件夹** — 去掉自动恢复逻辑

### 体验
- 热加载翻译统一为"热加载"
- PDF 导出去掉页眉页脚 + 隐藏右下角链接
- 设置面板新增语言选择器

---

## v2.1.0 — 行内编辑、Wiki 链接、Callout (2026-06-11)

### 新增
- **行内编辑** — 点击预览中任意区块原地编辑（⌘⇧E）
- **Wiki 链接** — `[[filename]]` 跨文件链接 + 反向链接面板
- **Callout 块** — Obsidian 风格 `> [!NOTE]`、`> [!WARNING]`、`> [!TIP]` 等
- **UI 打磨** — 界面细节优化

---

## v2.0.0 — 内置编辑器、文件管理 (2026-06-05)

### 新增
- **全屏编辑器** — 直接编辑文件，支持查找替换（⌘F），保存（⌘S）
- **文件管理** — 创建/删除文件，无需离开应用
- **PDF 导出** — 打印输出
- **命令面板** — ⌘K 搜索文件和内容

---

## v1.0.0 — 初始版本 (2026-05-17)

- Chrome 扩展（MV3）首个可用版本
- 本地 markdown 阅读器：侧边栏文件树、GFM 渲染、代码高亮、主题切换
- 零依赖、零构建、完全离线
